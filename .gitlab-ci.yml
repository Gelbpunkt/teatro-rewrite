image: "quay.io/podman/testing"

before_script:
  - podman --version

stages:
  - Test compile

test-compile:
  stage: Test compile
  script:
    - podman build --runtime crun --storage-driver vfs -t teatro:latest .
